<h1>Componentes</h1>

<table class='table'>
  <thead>
    <tr>
      <th>Nombre</th>
      <th>Descripcion</th>
      <th>Sección</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @componentes.each do |componente| %>
      <tr>
        <td><%= componente.nombre %></td>
        <td><%= componente.descripcion %></td>
        <td><%= componente.seccione.nombre if componente.seccione %></td>
        <td><%= link_to 'Ver', componente %></td>
        <td><%= link_to 'Editar', edit_componente_path(componente) %></td>
        <td><%= link_to 'Eliminar', componente, method: :delete, data: { confirm: 'Estas seguro(a)?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Crear Componente', new_componente_path, class:'btn btn-primary' %>
